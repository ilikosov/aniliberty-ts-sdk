{
  "name": "aniliberty-ts-sdk",
  "version": "1.0.0",
  "description": "Production-ready TypeScript SDK for the AniLiberty OpenAPI v1 API.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "generate:types": "node scripts/generate-types.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
